import { IcheckDirective } from './icheck.directive';

describe('IcheckDirective', () => {
  it('should create an instance', () => {
    const directive = new IcheckDirective();
    expect(directive).toBeTruthy();
  });
});
