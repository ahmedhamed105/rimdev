import { TestBed } from '@angular/core/testing';

import { FileupdatepassportService } from './fileupdatepassport.service';

describe('FileupdatepassportService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: FileupdatepassportService = TestBed.get(FileupdatepassportService);
    expect(service).toBeTruthy();
  });
});
